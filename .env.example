# Environment variables placeholder
# VITE_API_URL=http://localhost:8080

# Supabase (server-only — used by /api handlers, never exposed to the client bundle)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Resend (transactional email)
RESEND_API_KEY=
RESEND_FROM_EMAIL="T3 AI Works <hello@yourdomain.com>"
